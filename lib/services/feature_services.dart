// ignore_for_file: unused_field

class FeatureServices {
  // Initialize Network Service class
  // final HttpDioClient _apiService = HttpDioClient();

  Future<void> login(String email, String password) async {
    try {
      // Make a network call with _apiService
    } catch (e) {
      // Handle Errors
    }
  }
}
